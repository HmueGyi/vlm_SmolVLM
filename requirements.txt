# Requirements for SmolVLM demos
# Recommended Python: 3.10+

# NOTE: Install torch/torchvision separately to match your CUDA (or CPU) setup.
# Example (CUDA-enabled PyTorch) -- install the correct wheel from pytorch.org:
#   pip install torch==2.2.0 torchvision==0.17.0 --index-url https://download.pytorch.org/whl/cu118
# Or install CPU-only PyTorch via the official PyTorch instructions.

# Core libraries
torch>=2.0.0
torchvision>=0.15.0
transformers>=4.31.0
accelerate>=0.19.0
huggingface-hub>=0.14.1
safetensors>=0.4.0

# Image / vision
Pillow>=9.0.0
opencv-python>=4.7.0

# Tokenizers / text
sentencepiece>=0.1.98
tokenizers>=0.13.2

# ONNX Runtime (CPU or GPU builds available)
# onnxruntime>=1.15.0
# If you need the GPU-enabled ONNX runtime, install the appropriate package for your platform
# (e.g. onnxruntime-gpu). See https://onnxruntime.ai/ for details.

# Optional / advanced (only if you need quantization or low-RAM loading)
# bitsandbytes>=0.39.0
# peft>=0.4.0

# If you run into installation issues, follow the official project install guides for PyTorch,
# Transformers, and ONNX Runtime.

websocket-client
# For the web demo, install the websocket client library.